# eJustice Portal Environment Variables
# Copy this file to .env for local development or set these on your hosting platform (Railway, Docker, etc.)

# Database Configuration
# For Railway: Set DATABASE_URL automatically; optionally override individual values
DB_HOST=localhost
DB_NAME=ejustice_portal
DB_USER=root
DB_PASS=
DB_PORT=3306

# Railway provides: DATABASE_URL=mysql://user:pass@host:port/dbname
# If set, it overrides the individual DB_* vars above
# DATABASE_URL=mysql://user:password@railway.railway.internal:3306/railway

# Encryption Key
# Generate a secure key: openssl rand -hex 32
# ⚠️ CRITICAL: Change this to a secure random value in production!
DOC_ENC_KEY=1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t1u2v3w4x5y6z7a8b9c0d1e

# Application Environment
APP_ENV=development
APP_DEBUG=false

# Notes for Railway Deployment:
# 1. Create a Railway project at railway.app
# 2. Connect your GitHub repo (or upload ZIP)
# 3. Add MySQL service (Railway > Add Service > MySQL)
# 4. Railway auto-injects DATABASE_URL; also set:
#    - DOC_ENC_KEY: Generate via `openssl rand -hex 32`
#    - APP_ENV: "production"
#    - APP_DEBUG: "false"
# 5. Deploy and run database migrations (see DEPLOYMENT.md)
